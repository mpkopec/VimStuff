1. Install the fonts with the powerline shell
2. Install python and Lua
	Lua needs compiled lua binaries (.dll and .exe) copied to the main Vim dir. Lua version has to be 5.3.
3. Always use tux builds, they handle Python well
